include<stdio.h>
int main()
{
int l, w, perimeter;
perimeter=2*(l+w);
printf("/n enter the value of l");
scanf("%d", &l);
printf("/n enter the value of w");
scanf("%d", &w);
printf("/n perimeter of rectangle");
return 0;
 }
 
